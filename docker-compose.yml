services:
  rabbitmq:
    image: rabbitmq:4-management
    container_name: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: user
      RABBITMQ_DEFAULT_PASS: pass
    networks:
      - micro_net

  gateway-migrate:
    build: .
    command: alembic upgrade head   # sovrascrive il comando di avvio eseguendo solo le migrazioni
    depends_on:
      - gateway_db
    networks:
      - gateway_net
    deploy:
      replicas: 1   # forza una sola istanza
    restart: "no"   # non viene riavviato

  gateway:
    build: .
    command: gunicorn -k uvicorn.workers.UvicornWorker app.main:app --workers 4
    ports:
      - "8000:8000"
    depends_on:
      - gateway_db
      - gateway-migrate
    networks:
      - gateway_net
      - micro_net
    restart: unless-stopped

  gateway_db:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: gateway
    ports:
      - "5431:5432"
    volumes:
      - gateway_db_data:/var/lib/postgresql/data
    networks:
      - gateway_net
    restart: unless-stopped

volumes:
  gateway_db_data:

networks:
  gateway_net:
  micro_net:
    driver: bridge